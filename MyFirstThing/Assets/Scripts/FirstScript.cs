using System.Collections;// namespaces, grabbing information from libraries
using System.Collections.Generic;
using UnityEngine;//these are library of commands for unity

public class FirstScript : MonoBehaviour//name must match name of the file, monobehavior is a list of commands (may not be a list)
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hello There, welcome to my game!!!");
    }

    // Update is called once per frame
    void Update()//is the heart of the game
    {
        
    }
}
